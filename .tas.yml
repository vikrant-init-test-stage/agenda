---
framework: mocha
preRun:
     command:
        - yarn install
        - yarn run pretest
preMerge:
  pattern:
    - "./test/*.js"
postMerge:
  pattern:
    - "./test/*.js"
postRun:
     command:
        - yarn --version
version: 1.0
